{
  "name": "elevate-3",
  "version": "1.0.0",
  "private": true,
  "description": "E-Cell presents Elevate 26 / Elevate 3.0: a classic Clash of Clans village for the hackathon and business pitch competition",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "node scripts/prepare-deploy.mjs && npm run check",
    "check": "node scripts/check.mjs && node scripts/check-world.mjs && node scripts/check-mobile.mjs",
    "vendor": "node scripts/vendor.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "three": "0.180.0"
  }
}
